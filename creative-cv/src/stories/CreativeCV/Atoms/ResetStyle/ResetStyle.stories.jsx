import React from "react";

import { ResetStyle } from "./ResetStyle";

export default {
  title: "Atoms/ResetStyle",
  component: ResetStyle,
};

const Template = (args) => <ResetStyle {...args} />;

export const Reset = Template.bind({});

Reset.args = {};
